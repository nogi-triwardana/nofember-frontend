import styled from '@emotion/styled';
import { motion } from 'framer-motion';

export const THeadStyled = styled(motion.thead)`
  background-color: #bfd1d9;
`;

export const ThStyled = styled(motion.th)`
  font-weight: 600;
  padding: 8px;
`;